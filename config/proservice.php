<?php

/*
|--------------------------------------------------------------------------
| پرو سرویس
|--------------------------------------------------------------------------
|
| مشتری برای هر ساختمانش یک «پرونده» می‌سازد و روی آن پرونده قرارداد
| سرویس می‌بندد. دو طرح وجود دارد:
|
|   periodic   سرویس دوره‌ای — تکنسین طبق برنامه سر می‌زند، خرابی‌های
|              معمول داخل قرارداد است.
|   on_demand  سرویس موردی — تکنسین فقط وقتی خرابی ثبت شود می‌رود و
|              بابت همان یک بار فاکتور صادر می‌شود.
|
*/

return [

    /*
    | دوره‌های اشتراک. months برای محاسبه‌ی تاریخ انقضا استفاده می‌شود.
    | قیمت‌ها به تومان‌اند و «به ازای هر دستگاه آسانسور در ماه» حساب
    | می‌شوند، چون هزینه‌ی واقعی سرویس با تعداد کابین بالا می‌رود.
    */
    'terms' => [
        'monthly'   => ['label' => 'یک‌ماهه',  'months' => 1,  'discount_percent' => 0],
        'quarterly' => ['label' => 'سه‌ماهه',  'months' => 3,  'discount_percent' => 5],
        'yearly'    => ['label' => 'یک‌ساله',  'months' => 12, 'discount_percent' => 15],
    ],


    'plans' => [

        'periodic' => [
            'label'            => 'سرویس دوره‌ای',
            'description'      => 'تکنسین طبق برنامه‌ی ماهانه سر می‌زند و آسانسور را سرویس می‌کند.',
            // بازدید دوره‌ای در هر ماه
            'visits_per_month' => (int) env('PROSERVICE_VISITS_PER_MONTH', 1),
            // اجاره‌ی ماهانه به ازای هر دستگاه
            'monthly_fee'      => (int) env('PROSERVICE_PERIODIC_MONTHLY_FEE', 900000),
        ],

        'on_demand' => [
            'label'            => 'سرویس موردی',
            'description'      => 'بازدید دوره‌ای ندارد؛ هر بار که خرابی ثبت کنید تکنسین می‌آید و همان بار فاکتور می‌شود.',
            'visits_per_month' => 0,
            'monthly_fee'      => (int) env('PROSERVICE_ON_DEMAND_MONTHLY_FEE', 0),
        ],

    ],


    /*
    | نحوه‌ی تعیین تکنسین روی یک پرونده.
    |
    |   dedicated    تکنسین ثابت، روی خود قرارداد ثبت می‌شود.
    |   assigned     ما برای هر کار تکنسین می‌گذاریم.
    |   per_request  مشتری هر بار خودش از فهرست انتخاب می‌کند.
    */
    'technician_modes' => [
        'dedicated'   => 'تکنسین اختصاصی ثابت',
        'assigned'    => 'انتخاب توسط آسانسور پرو',
        'per_request' => 'هر بار خودم انتخاب می‌کنم',
    ],


    // سهم پلتفرم از هر فاکتور خرابی (درصد).
    'commission_percent' => (int) env('PROSERVICE_COMMISSION_PERCENT', 15),


    /*
    | بیمه‌نامه. قرارداد می‌تواند بیمه‌نامه‌ی واقعی داشته باشد که مقادیرش
    | را ادمین از روی بیمه‌نامه‌ی صادرشده وارد می‌کند. چند روز مانده به
    | انقضا هشدار داده شود:
    */
    'insurance_expiry_warning_days' => (int) env('PROSERVICE_INSURANCE_WARNING_DAYS', 30),


    // چند روز مانده به پایان قرارداد به مشتری یادآوری تمدید بدهیم.
    'renewal_reminder_days' => (int) env('PROSERVICE_RENEWAL_REMINDER_DAYS', 14),

];
