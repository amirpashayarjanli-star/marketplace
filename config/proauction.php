<?php

/*
|--------------------------------------------------------------------------
| پرو مزایده
|--------------------------------------------------------------------------
|
| مکانیک این بخش «مناقصه» است: کارفرما پروژه‌ی آسانسوری را می‌گذارد،
| شرکت‌ها/تولیدکنندگان/تکنسین‌های تاییدشده پیشنهاد قیمت + زمان تحویل
| می‌دهند و کارفرما برنده را انتخاب می‌کند. همه‌ی مبلغ‌ها عمومی است.
|
*/

return [

    // کارمزد ثبت هر پیشنهاد، درصدی از مبلغ همان پیشنهاد.
    // موقع ثبت پیشنهاد از کیف‌پول کاربر کسر می‌شود.
    'fee_percent' => (float) env('PROAUCTION_FEE_PERCENT', 5),

    // اگر پیشنهادی در این بازه‌ی پایانی ثبت شود، مهلت مزایده
    // به همین اندازه از «الان» تمدید می‌شود (ضد اسنایپ).
    'anti_snipe_minutes' => (int) env('PROAUCTION_ANTI_SNIPE_MINUTES', 10),

    // محدوده‌ی مجاز مدت یک مزایده هنگام ساخت توسط کارفرما.
    'min_duration_hours' => (int) env('PROAUCTION_MIN_DURATION_HOURS', 24),
    'max_duration_days'  => (int) env('PROAUCTION_MAX_DURATION_DAYS', 30),

    /*
    | مشاوره‌ی پیش از انتشار.
    |
    | مزایده‌ی کارفرما (source=onsite) با وضعیت «در انتظار مشاوره» ساخته
    | می‌شود. کارفرما باید با این شماره تماس بگیرد؛ بعد از مشاوره، مدیر
    | آن را ثبت و مزایده را منتشر می‌کند. مناقصه‌های ستاد از این مرحله
    | معافند چون کارفرمای داخلی ندارند.
    |
    | consultation_fee = هزینه‌ی مشاوره به تومان. اگر بزرگ‌تر از صفر باشد،
    | مزایده تا وقتی این مبلغ از کیف‌پول کارفرما پرداخت نشده منتشر نمی‌شود.
    | صفر = مشاوره رایگان، فقط تماس تلفنی.
    */
    'consultation_phone' => env('PROAUCTION_CONSULT_PHONE', '09102530106'),
    'consultation_fee'   => (int) env('PROAUCTION_CONSULT_FEE', 0),

    // پیامک «درخواست تماس» به این شماره‌ها می‌رود (با کاما جدا کن).
    'admin_mobiles' => array_filter(explode(',', (string) env('PROAUCTION_ADMIN_MOBILES', ''))),

];
